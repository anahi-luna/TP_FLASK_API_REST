version: '3.9'

services:
    api:
      build:
        context: ..
        dockerfile: deploy/Dockerfile
      container_name: contactos_api
      ports:
        - "5000:5000"
      volumes:
        - ../backend:/app

